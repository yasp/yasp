if (typeof yasp == 'undefined') yasp = { };

(function() {
    /**
     * @class Checks the code for syntactic correctness
     */
    yasp.Checker = function() {

    };

    yasp.Checker.prototype.pass = function(assembler, input) {
        // TODO Implement this
        return input;
    }
})();