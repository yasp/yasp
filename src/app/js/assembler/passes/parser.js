if (typeof yasp == 'undefined') yasp = { };

(function() {
    /**
     * @class Parses the source code and generates the "AST"
     */
    yasp.Parser = function() {

    };

    yasp.Parser.prototype.pass = function(assembler, input) {
        // TODO Implement this
        return input;
    }
})();