if (typeof yasp == 'undefined') yasp = { };

(function() {
    /**
     * @class executes all the directives
     */
    yasp.PreProcessor = function() {

    };

    yasp.PreProcessor.prototype.pass = function(assembler, input) {
        // TODO Implement this
        return input;
    }
})();